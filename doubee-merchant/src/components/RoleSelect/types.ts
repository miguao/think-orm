export type { Role, RoleParam } from '@/api/system/role/model';

/**
 * 组件文案
 */
export interface RoleSelectLocale {
  title: string;
  clear: string;
  selected: string;
  selectedUnit: string;
  labelRoleName: string;
  labelRoleCode: string;
  labelComments: string;
  labelCreateTime: string;
}
