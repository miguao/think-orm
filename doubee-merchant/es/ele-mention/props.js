import { mentionProps as mentionProps$1 } from "element-plus";
import { mentionEmits } from "element-plus";
const mentionProps = {
  ...mentionProps$1,
  /** 数据 */
  options: [Array, Function]
};
export {
  mentionEmits,
  mentionProps
};
